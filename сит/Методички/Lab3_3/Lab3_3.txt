
                  ЛАБОРАТОРНАЯ N 3 , третий курс
				  Серверные интернет технологии
				  
1. Изменить код прилагаемого html документа таким образом
   чтобы при введении пользователем в дробном десятичном числе
   запятой вместо точки сценарий это исправлял бы.
   ( Строка - объект класса String, используйте
     метод замены подстрок)
	 
	 
2. Создайте html документ со сценарием . 
    Документ генерирует и отображает таблицу значений sinx, cosx, tgx для
	x = 0, 15, 30, 45, 60, 75, 90 градусов.
	
3.  Написать функцию, получающую на вход строку с записью двоичного числа
    и переводящую её в десятичное число. Примеры с '110011'	и '111'
        

4. Написать сценарий задающий функцию bisect, которая	
   получает на вход параметры:
   
     а. функцию equ - вычисляющую некоторое выражение.
	 
	 б.  нижнюю границу a отрезка, содержащего корень функции equ.
	 
	 в.  верхнюю границу b отрезка, содержащего корень функции equ.
	 
	 г.  eps - допустимая погрешность.
	 
   и методом бисекции находит корень функции equ на отрезке
     [a,b] с точностью  eps.  
	 
   Проверить для 
     1. equ = x^2 - cosx, 
	 2. equ = x^4 + 1.5 - sinx
	 a=0, b=1, eps= 0.001	 


4. Написать код, который создаёт часы,
   каждую секунду выводит текущее время до секунд
   К часм, минутам,секундам меньше 10 при выводе 
   добавлять впереди 0


5. Изучите информацию об объектах javascript
   https://learn.javascript.ru/object
   Имеется массив объектов:

    let user = [
    {
       name: "John",
       age: 30
    },
    {
      name: "Bob",
      age: 21
    },
   {
      name: "Anna",
      age: 19
    }
]
   
   Получите из этого массива объект, где name == "Bob" 
   и сохраните это в какой-либо переменной.



6.   
   Напишите функцию JavaScript, чтобы получить все значения 
   свойств (keys) объекта. 
   let an_obj = { 100: 'a', 2: 'b', 7: 'c' };
 
7. 
  а. Создайте фукцию которая для каждого числового
     свойства объекта добавит в объект новое свойство
     со значением 	равным десятичному логарифму этого числового
     значения , например log_width, log_height
	 
   b.  Создайте фукцию которая  удалит из объекта все его
       нечисловые  свойства
       Результат продемонстрируйте в браузере
       Тестовый объект :   	   
   
        let menu = {
            width: 200,
            height: 300,
            title: "My menu"
        };



