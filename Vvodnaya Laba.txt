
/*Задание_1*/
    
/* Пример_1*/
/*    
#include <iostream>
using namespace std;
    
int main()
{
    int var1;
    int var2;
    var1 = 20;
    var2 = var1 + 10;
    cout << "var1 + 10 ravno";
    cout << var2 << endl;
    
    return 0;
}
*/

/* Пример_2 */
/*
#include <iostream>
using namespace std;

int main()
{
    char charvar1 = 'A';

    char charvar2 = '\t';
    cout << charvar1;
    cout << charvar2;
    charvar1 = 'B';

    cout << charvar1;
    cout << '\n'; 

    return 0;
}
*/
    /* Пример_3 */
/*
#include <iostream>
using namespace std;

int main()
{    
    float rad;
    const float PI = 3.14159F;
    cout << "Vvedite radius okryzhnosti: ";
    cin >> rad;
    float area = PI * rad * rad;
    cout << "Ploshad' kryga ravna " << area << endl;

    return 0;
}
*/

    /*Задание_2 Var5 */
/*
    #include <iostream>
    #include <cstring>
    using namespace std;

    class Kvitanziya // определение класса
    {
    private:
        int number;
        char date[15]; // string date;
        float amount;

    public:
    int getNumber() {
        return number;
    }

    const char* getDate() {
        return date;
    }

    float getAmount() {
        return amount;
    }

    void setNumber(int num) {
        number = num;
    }

    void setDate(const char* dat) {
        strncpy(date, dat, sizeof(date) - 1);
        date[sizeof(date) - 1] = '\0';
    }

    void setAmount(float amt) {
        amount = amt;
    }
};

int main() {
    Kvitanziya kv;

    kv.setNumber(1234);
    kv.setDate("01.01.2023");
    kv.setAmount(15000);

    cout << "Nomer kvitanzii: " << kv.getNumber() << endl;
    cout << "Data: " << kv.getDate() << endl;
    cout << "Summ: " << kv.getAmount() << endl;

    return 0;
}
*/
